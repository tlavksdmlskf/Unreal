#include <stdio.h>

int main()
{
	int arr[10] = {2, 10, 30, 21, 34, 23, 53, 21, 9, 1};
	
	for(int i = 0; i < 10; i++)
	{
		if ((i + 1) % 2 == 0) 
		{
		printf("배열의 %d번째 원소 : %d \n", i + 1, arr[i]);
		}
	}
}
